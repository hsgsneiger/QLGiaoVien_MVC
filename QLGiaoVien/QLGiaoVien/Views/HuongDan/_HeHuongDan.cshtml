@model List<DAO.GuideSystemDAO> 
<label for="HeHuongDan_id">Hệ Hướng Dẫn</label>
<select class="form-control" id="HeHuongDan_id">
    @for(int i = 0; i < Model.Count; i++)
    {
        <option value='@Model[i].id'>@Model[i].TenHeHuongDan</option>
    }
</select>